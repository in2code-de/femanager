<f:translate key="emailUpdateRequestSalutation" />

<f:translate key="emailUpdateRequestText1"  arguments="{0:user.username}"/>

<f:translate key="emailUpdateRequestText2" />





<f:for each="{changes}" as="change" key="property">
	<f:translate key="emailUpdateRequestField" />: <f:translate key="tx_femanager_domain_model_user.{property}" />

	<f:translate key="emailUpdateRequestOld" />:
	<f:if condition="{0:property} == {0:'password'}">
		<f:else>
			<f:if condition="{change.old}">
				<f:then>
					<f:if condition="{0:property} == {0:'gender'}">
						<f:then>
							<f:translate key="tx_femanager_domain_model_user.gender.item{change.old}" />
						</f:then>
						<f:else>
							<f:if condition="{0:property} == {0:'dateOfBirth'}">
								<f:then>
									<f:format.date format="{f:translate(key:'tx_femanager_domain_model_user.dateFormat')}">{change.old}</f:format.date>
								</f:then>
								<f:else>
									{change.old}
								</f:else>
							</f:if>
						</f:else>
					</f:if>
				</f:then>
				<f:else>
					[<f:translate key="empty" />]
				</f:else>
			</f:if>
		</f:else>
		<f:then>
			******
		</f:then>
	</f:if>

	<f:translate key="emailUpdateRequestNew" />
	<f:if condition="{0:property} == {0:'password'}">
		<f:else>
			<f:if condition="{0:property} == {0:'gender'}">
				<f:then>
					<f:translate key="tx_femanager_domain_model_user.gender.item{change.new}" />
				</f:then>
				<f:else>
					<f:if condition="{0:property} == {0:'dateOfBirth'}">
						<f:then>
							<f:format.date format="{f:translate(key:'tx_femanager_domain_model_user.dateFormat')}">{change.new}</f:format.date>
						</f:then>
						<f:else>
							{change.new}
						</f:else>
					</f:if>
				</f:else>
			</f:if>
		</f:else>
		<f:then>
			******
		</f:then>
	</f:if>
</f:for>

<f:translate key="emailUpdateRequestLinkConfirm" />:
<f:uri.action action="confirmUpdateRequest" controller="Edit" absolute="1" arguments="{user:user, hash:hash}" />

<f:translate key="emailUpdateRequestLinkRefuse" />:
<f:uri.action action="confirmUpdateRequest" controller="Edit" absolute="1" arguments="{user:user, hash:hash, status:'refuse'}" />

<f:translate key="emailUpdateRequestLinkSiltentRefuse" />:
<f:uri.action action="confirmUpdateRequest" controller="Edit" absolute="1" arguments="{user:user, hash:hash, status:'silentRefuse'}" />
